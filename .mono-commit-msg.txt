Fix Case-studies-tab white screen (missing PenTool import) + embed activities inline in course (ASCII-only)

- Crash: CourseCasesTab used a PenTool icon that was never imported in CourseDetail, so rendering an attached case (right after adding one) hit an undefined component and blanked the page. esbuild did not catch it (lucide is an external package). Imported PenTool (and ChevronDown).
- Embed: the in-course Activities tab now embeds each interactive inline via ActivityPlayer (Preview/Hide expander) instead of only linking out, so added deliverables render within the course. Activity list response already carries html/embedUrl.

VALIDATION: esbuild bundle clean on CourseDetail.tsx.
