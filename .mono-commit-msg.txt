Fix Enza learner logins: self-healing ensureTestLearners guarantees the 4 test accounts exist (ASCII-only)

Login as enza@student1.test failed because a partially-seeded cohort left the org existing but its learners incomplete, so the old rename-only step had nothing (or too few) to rename. Replaced it with ensureTestLearners(), which GUARANTEES the four demo accounts: it reuses learners already in the org (renaming in creation order so student1 is advanced), CREATES any that are missing, sets email enza@studentN.test + password Enzatest123 (hashed) + active, adds them to the cohort roster, and enrols them into the cohort courses with level-appropriate progress if they have none yet. Runs on every Seed Enza Cohort click (create path and the already-exists branch), so clicking once now repairs a broken cohort into a loggable state. Toast reports the exact emails + password.

VALIDATION: esbuild clean on enzaCohortSeed.ts.
