Org-hub finance on real data + render real orgs (Partner Hub convergence stage 2b) (ASCII-only)

The per-org hub's Billing/Funding/Documents tabs (and the overview/settings finance stats) rendered from the client-side mock (orgDetail d.sub/d.plan/d.funders/d.openInvoices, and mock-seeded invoices/docs state), and mark-paid/upload mutated local state only. Rewired them to REAL data: resolve the org's partner via /organisations/:id, then read /partners/:pid/billing, /funding, /documents filtered to this org; mark-paid now PATCHes /partners/:pid/invoices/:id and upload POSTs /partners/:pid/documents, each invalidating its query.

Also fixed a latent 404: a REAL org id isn't in the client mock, so orgDetail(h,id).org was undefined and the page showed 'Organisation not found' - which the now-real Organisations overview would hit on every click. The page now resolves org identity from /organisations/:id (orgObj) and renders. (People/members still read the mock - a separate follow-up; finance/funding/docs are now real.)

VALIDATION: esbuild clean on PartnerOrgHub.tsx.
