Course clone + link activities into a course (ASCII-only)

- Clone: new backend POST /courses/:id/clone deep-copies the course + its modules, beats, assignments and course-linked interactive activities into a fresh draft (author roles; super_admin passes). Clone button on the catalog cards.
- Link activities to a course: the in-course Add-interactive / Add-case buttons now pass ?courseId; ActivitiesAdmin reads it and preselects the course on new activities (+ hint banner) so a new activity is linked to the course.

NOTE: this deploy also carries the earlier uncommitted set (course authoring: edit course + assignment builder + in-course authoring bar; Learning Hub persistence backend; create-course UI; ribbon/catalog polish). Learning Hub migration (praxis/migration-learning-hub.sql) still pending; nothing calls those tables from the UI, so safe.

VALIDATION: esbuild clean on courses.ts, Courses.tsx, CourseDetail.tsx, ActivitiesAdmin.tsx.
