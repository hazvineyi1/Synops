Security: tuned Content-Security-Policy (report-only) for app + embed routes

Adds the CSP the code TODO called for, shipped in REPORT-ONLY mode so it blocks nothing on the live app - the browser only reports violations. Route-aware frame-ancestors: the app may be framed only by itself (anti-clickjacking), while /c/:token and /a/:token stay embeddable on external sites (they are designed to be). Policy allows what the SPA actually uses (inline styles, https images/branding, google fonts, same-origin scripts/connect/frames). Next step after observing clean reports: flip the header from Content-Security-Policy-Report-Only to Content-Security-Policy to enforce. File: app.ts.
