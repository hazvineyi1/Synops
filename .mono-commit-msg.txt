Document Library: real Word + PDF files now downloadable from the platform (ASCII-only)

The standard legal pack is now truly uploaded on the platform, not just viewable HTML. Baked the actual letterhead files (dash-free, fillable Word + PDF for all 10 documents) into api-server/src/data/documentFiles.json and added GET /document-templates/:key/download?format=docx|pdf (any authed user; streams the real file with attachment headers). Super-admin Document Library page gains Word + PDF download buttons per document; partner Documents and Filing gains Word + PDF download buttons on template-backed rows (so a partner downloads the exact letterhead file that was sent to them). The Word files are fillable (every [placeholder] is a click-to-type content control, every checkbox is clickable).

VALIDATION: esbuild clean on documentTemplates.ts (bundles documentFiles.json), AdminDocumentTemplates.tsx, PartnerDocuments.tsx.
