Org Hub: track last password reset (date + channel) on the learner drawer (ASCII-only)

NO MIGRATION REQUIRED. Frontend only, seeded partner data.

Member now carries lastReset { at, channel }. Sending a reset via Email or WhatsApp records the timestamp and channel; the drawer shows Last reset <date> via <email/WhatsApp> under the reset buttons, or No password reset on record. Learners are seeded with a realistic prior-reset history (some never reset).

VALIDATION: parse-check + local-resolution bundle clean.
