Org Hub: learner drawer actions give visible feedback + archive/delete account (ASCII-only)

NO MIGRATION REQUIRED. Frontend only, seeded partner data.

BUGFIX: reset-password, WhatsApp/email resend and login-help buttons appeared dead because their confirmation used the page-level flash banner, which renders BEHIND the modal. Added an in-drawer confirmation line so every action (role change, reset via email/WhatsApp, resend login help, suspend/reactivate) reports where the admin can see it.

NEW: Archive account (soft - keeps the record, removes access, reversible via Restore) and Delete account (permanent, behind an inline Confirm/Cancel with a warning). Member status type gains archived; status chip shown in the drawer header and the People table.

VALIDATION: parse-check + local-resolution bundle clean; Trash2 icon confirmed.
