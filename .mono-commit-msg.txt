Testing: expand gradebook engine unit suite (money-path coverage)

Adds 10 unit tests to gradebookEngine.test.ts covering the grading logic that feeds credentials and off-track alerts: auto-score-from-completion (fill + averaging + no-overwrite of explicit grades), weighted overall (summative/formative split + per-category weights), letter bands, the at-risk band, and trend-down detection. Also fixes a latent type gap: the col() test factory now sets the required gradeType field. 17 tests total, all green; runs in CI via the existing build-praxis vitest gate. No runtime code change. Files: lib/gradebookEngine.test.ts.
