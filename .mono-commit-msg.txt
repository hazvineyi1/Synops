Org-hub People on real data (Partner Hub convergence complete) (ASCII-only)

The per-org People/roster read the client-side mock (orgDetail d.admins/d.coaches + orgLearners), and add/role/suspend/delete mutated local state only. Rewired the roster to REAL data: it now reads /partners/:pid/members filtered to this org (org_admin/coach/learner). Add member POSTs /organisations/:orgId/members, role change PATCHes, remove DELETEs - each invalidating the roster query so it persists. The settings 'primary admin' field now shows the real org_admin.

Status (suspend/archive) stays an optimistic local change - it is a super-admin/platform-console op with no org-scoped endpoint - and the learner personal-info drawer (fabricated PII) simply doesn't render for real accounts, which is correct.

With finance (prior commit) and now People real, the whole per-org hub is off the mock. Remaining mock-ish per-org rollups (courses/coaching/gradebook summary tiles) are separate follow-ups.

VALIDATION: esbuild clean on PartnerOrgHub.tsx.
