Bulletproof Enza test logins: accounts guaranteed first, progress best-effort, self-verifying toast (ASCII-only)

Hardens the learner-login guarantee so a partial/failed seed can never leave enza@studentN.test unusable. ensureTestLearners now runs in two passes: PASS 1 guarantees the four accounts (updates EVERY row on each target email so a stray duplicate cannot shadow the real one with a null password at login; else adopts the i-th learner; else creates) with password Enzatest123 (hashed) + active; PASS 2 does enrolments/progress best-effort inside try/catch with onConflictDoNothing, so a cosmetic hiccup can never undo the accounts. The create path's rich delivery seeding (steps 4-8) is now wrapped in try/catch so it always reaches the login step. Finally verifyTestLearners re-reads each account and confirms the password actually validates + status active + uniqueness, and the endpoint returns that as the toast (e.g. 'Logins 4/4 verified ... enza@student1.test: OK'), so any remaining problem is self-diagnosing.

VALIDATION: esbuild clean on enzaCohortSeed.ts.
