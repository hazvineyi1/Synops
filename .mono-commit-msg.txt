Migration tooling: add drizzle-kit generate/migrate scripts + out dir

Production-hardening step 1 (schema migrations). drizzle.config.ts gains out:'./migrations' + strict/verbose; @workspace/db package.json gains generate, migrate and migrate:check scripts alongside the existing push. This is repo/dev tooling only - deploying it does NOT touch the database at runtime. Adoption on the live DB (generate baseline, mark applied, apply on deploy, retire boot heals) is documented in the Email-and-Migrations hardening runbook and should be run on staging first. Email was already code-complete (Resend in lib/email.ts); it only needs RESEND_API_KEY + EMAIL_FROM + DNS. Files: lib/db/drizzle.config.ts, lib/db/package.json.
