Partner Hub mock->real: Overview, Partners list, LearningHub targets (ASCII-only)

The last high-traffic Partner Hub screens still read the client mock (which now returns an empty hub, so they showed blanks). Rewired to real endpoints: PartnerOverview aggregates /organisations + /partners/:id/billing + /funding + /delegated-admins (real MRR, seats, funded seats, outstanding, per-org cards, finance/funder snapshots); the removed 'recent activity' card had no real partner-audit source. PartnerPartners (super-admin partner selector) lists the real /partners and registers their names so getPartnerHub resolves correctly. LearningHub's assign-to-partners targets come from real /partners (was the fake TalentForge/SkillBridge list).

Still mock (render empty/safe, flagged): PartnerAudit (needs a partner-scoped audit endpoint) and the client-side PartnerImpersonateView preview (redundant with the real /platform impersonation session).

VALIDATION: esbuild clean on PartnerOverview.tsx, PartnerPartners.tsx, LearningHub.tsx.
