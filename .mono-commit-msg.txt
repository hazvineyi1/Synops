Enza cohort: isolate each enrolment insert + report course counts in the toast (ASCII-only)

Logins now work but learners had no courses because PASS 2 wrapped all enrolments in one try/catch - so a single failing insert (e.g. a beat-progress drift) silently skipped the rest. Each enrolment and each progress insert is now isolated in its own try/catch, so one failure can never block a learner's other course enrolments. The verification now also reports how many courses Enza has assigned and how many each learner is enrolled in (e.g. 'Enza has 15 courses assigned. enza@student1.test: OK 4c'), so if courses still do not appear the toast says whether the gap is missing assignments or the enrolment step.

VALIDATION: esbuild clean on enzaCohortSeed.ts.
