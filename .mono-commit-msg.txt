Video tab: render the narrated slide lesson (with images) instead of a 'no file' placeholder (ASCII-only)

The hub Video tab only checked for an uploaded videoUrl and otherwise showed 'Video file not uploaded yet' - ignoring the NotebookLM-style slide lesson. It now renders the SlideLesson (which carries per-slide images) whenever the video beat has slides but no uploaded file, so the Video section shows real visuals. Matches what the in-beat and prominent players already do.

VALIDATION: esbuild clean on ModuleViewer.tsx.
