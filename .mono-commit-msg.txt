Communications on live data: real partner_announcements broadcast history (ASCII-only)

Partner Communications now persists. New partnerComms router (mounted): GET/POST/DELETE /partners/:id/announcements (self-create table, additive; super admin any partner, partner_admin own). Send records the broadcast (subject, body, channel, audience, recipient count); the Sent tab reads real history; recipient counts + audience (all/org/role) computed from the partner's REAL members; delete persists. New schema partner_announcements.ts. Actual in-app/email delivery remains the messaging step.

VALIDATION: esbuild clean on partnerComms.ts, routes/index.ts, db index (partner_announcements exported), PartnerComms.tsx.
