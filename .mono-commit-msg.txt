Admin: real password reset + temp password + test accounts, learner pool, course categories

(1+2) The org member modal reset was a mock (toast only, no call). Now real: POST /partners/:pid/members/:userId/credentials mode=link mints a copyable set-password link (emails if a provider is configured) or mode=temp sets a shown temporary password and revokes live sessions. (2) Create test learners with a known login password. (3) Partner LEARNER POOL: POST/GET /partners/:pid/learners + assign endpoint create learners at the partner level (org null) then assign them into organisations; new Learner pool UI on the Accounts page with a test-account toggle. (4) Course catalog now derives a category (IT & Digital, Business & Finance, Hospitality & Tourism, Green Energy, Trades, Care & Education, Compliance, Customer Service & BPO) from title/tags and adds filter chips - no schema change. Frontend + platform.ts endpoints only.
