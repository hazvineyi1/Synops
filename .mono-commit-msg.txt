Real platform alerts + health on the Overview (derived from live data) (ASCII-only)

GET /platform/alerts computes real attention-needed signals: funding agreements expiring within 60 days or expired, unpaid invoices, documents flagged action-required, partners still onboarding, courses in draft; plus real health (learner count, active enrolments). PlatformOverview gains an Attention-needed panel + a real health line, replacing the last seeded coaching-health/alerts placeholders. Missing tables count as zero.

VALIDATION: esbuild clean on platform.ts, PlatformOverview.tsx.
