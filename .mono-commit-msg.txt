Fix org courses crash + demo learner login

(1) Partner org courses page crashed with 'Cannot read properties of undefined (reading includes)': the courses table did classes.filter(cl => cl.courseIds.includes(c.id)) but ClassRow has no courseIds field, so any org with a class threw at render. Guarded with ((cl).courseIds ?? []). (2) Demo learner button returned 503 'not provisioned': /auth/demo-login looked up a fixed email enza@student1.test that was never created (seeded learners use studentN@learner.enzaglobalmedia.co.za). The student path now falls back to a real ACTIVE learner inside the Enza partner (prefers one placed in an organisation, skips archived/deleted) - still narrow, never an arbitrary account. Demo admin already worked. Files: PartnerOrgHub.tsx, routes/auth.ts.
