Learning Hub increment C: rewire frontend from in-session state to the real API (ASCII-only)

learningHubStore now hydrates from and writes through /api/learning/* instead of holding seed data in memory: hydrates content/templates/assignments once on first use, and persists every mutation (addContent link, uploadContent file->Supabase Storage, review, delete, assignment toggle/set) to Postgres with optimistic cache updates. LearningHub upload path now does a real file upload (surfaces the storage-not-configured error until SUPABASE env is set); URL/link add persists. PartnerOrgHub 'Assigned from the Learning Hub' now reads the reactive loaded assignments/templates so it reflects real, persisted grants. Data survives reload.

NOTE: file uploads need SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY + a learning-hub bucket; templates/links/assignments persist with just DATABASE_URL.

VALIDATION: esbuild bundle clean on learningHubStore, LearningHub, PartnerOrgHub, CourseDevelopmentSuite.
