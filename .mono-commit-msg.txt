Landing = hero only; brand the sign-in page in the partner palette (ASCII-only)

Simplified the partner landing page to just the nav + hero (Connect.Create.Elevate / 'Growth comes from skilling your people' + Enter the platform) - removed the Explore-programmes button and the programmes/principles/stats/stakeholders/testimonials/footer sections. Enter the platform and Sign in now link to /sign-in?p=<slug> so the sign-in carries the brand. Rebuilt the sign-in page to follow the partner's colour palette: background from the brand primary (Enza near-black), the Sign in button and focus/links from the brand accent (Enza lime) with an auto-contrast text colour, and a white-backed logo chip - reading the brand from ?p=<slug> or the host brand. No more default indigo/navy.

VALIDATION: esbuild clean on PartnerLanding.tsx, SignIn.tsx.
