Partner Hub: last seeded widgets now real (engagement, login activity, delegated admins, settings profile) (ASCII-only)

Final pass making the remaining Partner Hub placeholders real. (1) Platform Overview: learner-engagement % + per-partner Commercials (monthly, funding) now from the real /platform/financials + /platform/alerts; removed the last Sample badges. (2) Login activity: new GET /platform/users/:id/login-activity (real login_events); Accounts drawer shows a real sign-in trail for super admin. (3) Delegated admins: new delegated_admins table + router GET/POST/PATCH/DELETE /partners/:id/delegated-admins; Accounts Delegated Admins tab persists (add/revoke), org dropdowns use real orgs. (4) Partner Settings profile saves name + billing contact via PATCH /partners/:id. New schemas delegated_admins.ts; engagementRate added to /platform/alerts.

VALIDATION: esbuild clean on platform.ts, delegatedAdmins.ts, routes/index.ts, db index (delegated_admins exported), PlatformOverview.tsx, PartnerAccounts.tsx, PartnerSettings.tsx, PartnerFunders.tsx.
