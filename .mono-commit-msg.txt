Studio: delete-draft endpoint + delete button on cards (ASCII-only)

- Backend: new DELETE /studio/scripts/:draftId gated to author roles; non-hub authors may only delete drafts in their own tenant, hub roles (super_admin/instructional_designer) any.
- Frontend: trash button on each Studio draft card (author roles only) with a confirm, then refetch; clears the accumulated test drafts.

VALIDATION: esbuild clean on Studio.tsx and studio.ts.
