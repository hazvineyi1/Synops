Partner Overview: move the Organisations card up (below the KPIs, above the Financial/Funders snapshots) (ASCII-only)

NO MIGRATION REQUIRED. Frontend only. Reordered the Partner Overview so Organisations - the main-admin map into each tenant - is high on the page instead of at the bottom.

VALIDATION: parse-check + local-resolution bundle clean; order confirmed.
