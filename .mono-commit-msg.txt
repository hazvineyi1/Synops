Studio cleanup: rename to Module Builder + lock down backend routes (ASCII-only)

- Renamed the page from Animation Studio to Studio - Module Builder with an accurate subtitle (turn source material into a lesson, publish as a module in a course).
- Backend hardening (studio.ts): generate-script, PATCH draft, and publish now require an author role (super_admin/partner_admin/org_admin/coach/instructional_designer) instead of any signed-in user; publish additionally checks canStaffActOnCourse for the target course, so you cannot publish a module into a course you do not staff. Reads stay auth-only.

VALIDATION: esbuild clean on Studio.tsx and studio.ts.
