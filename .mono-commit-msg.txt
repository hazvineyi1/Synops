Add-to-course from the deliverable pages: obvious per-card action on Activities and Cases (ASCII-only)

Previously placing a deliverable in a course was buried (Activities: a dropdown behind Edit tab; Cases: a published-only button labelled Gradebook), so it looked like there was no way to do it.
- Activities: every activity card gets an Add to course / Change course button + an In a course badge; opens a course+module picker that PATCHes /activities/:id (shows in the course Activities tab).
- Cases: relabelled Gradebook to Add to course, opened it to all authors, and drafts now show a disabled Add to course (publish first) so the path is visible.

VALIDATION: esbuild bundle clean on ActivitiesAdmin.tsx and Cases.tsx.
