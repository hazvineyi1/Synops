Enza branding: real logo + favicon + full colour palette applied to the partner hub (ASCII-only)

Completes Enza Global Media branding from their website. The seed brand kit now includes the real hosted logo (cropped-logo) and favicon plus the full palette (near-black #111 primary, signature lime #9CDF00 secondary, rust #D8613C accent, Heebo font). Refactored the brand write into an idempotent applyBrand() upsert that runs on EVERY provision - so clicking Provision Enza Global when the partner already exists now refreshes logo + favicon + colours instead of skipping. First-time provision still creates partner + org + faculty + 15 courses and assigns them to Enza.

VALIDATION: esbuild clean on enzaSeed.ts, platform.ts.
