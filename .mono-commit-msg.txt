Populate the 'In classes' column: classes API now returns courseIds

GET /organisations/:orgId/classes now returns courseIds[] per class (from org_class_courses, same query that already produced courseCount). PartnerOrgHub ClassRow gains courseIds and the org courses table uses (cl.courseIds ?? []).includes(c.id), so the 'In classes' column shows the real class chips instead of a dash. Files: routes/classes.ts, pages/partner/PartnerOrgHub.tsx.
