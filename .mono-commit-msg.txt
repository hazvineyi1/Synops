Reconcile billing active-seats to real learners (fix seats-vs-people discrepancy)

seedEnzaHub fabricated active seats as 80% of a made-up total, unrelated to real learners, so an org with 0 people showed 32/40 active seats. Now active seats = the org's actual active-learner count (users with role learner, status active, in that org); contracted total stays the plan size but is lifted if it sits below real usage. Made the seed self-correcting: when billing already exists it RECONCILES each subscription's active seats to reality instead of bailing, so re-clicking Seed Hub Data fixes existing rows. Added usersTable import.
