Partner Hub convergence stage 2: Organisations overview on real data (ASCII-only)

The Organisations overview page rendered entirely from the client-side mock (partnerHubData getPartnerHub/orgDetail/financeRollup) - fabricated MTN/Vodacom orgs, seats, funders and invoices that had nothing to do with the real partner. Rewired it to fetch the SAME real endpoints the individual Hub pages use: /organisations (partner-scoped), /partners/:id/billing, /funding, /delegated-admins, /members. Per-org cards now compute plan/seats/funders/coaches/delegated/open-invoices from those, and the top stats (orgs, total seats, delegated admins, outstanding) roll up the real billing. Create-organisation now POSTs /organisations (real) instead of pushing into the mock store. The overview now agrees with the hubs.

VALIDATION: esbuild clean on PartnerOrganisations.tsx.
