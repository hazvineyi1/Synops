Enrol confirmation + finish course-tab error handling + fix Coach materials tile (ASCII-only)

ENROL SAFETY: 'Enrol Now' fired an immediate, irreversible enrolment on a single click. Now it asks for confirmation first (names the course + notes it may count toward the training record).

COURSE TABS: completed the error-hang sweep for CourseDetail - the Assignments, Discussions and Groups tabs still gated their skeleton on '!data', so a 403 for an unenrolled learner hung forever. They now show 'Enrol in this course to view...' (unenrolled) or 'Could not load... please refresh' (real error) instead of an endless skeleton, matching the Modules/Announcements/Pages/People tabs.

COACH TILE: the Coach hub 'Materials' stat read the overview's materialCount (an unrelated figure - it showed 4 while the materials list was empty). It now counts the REAL uploaded materials from the same list the Materials page uses, so the tile and page always agree.

VALIDATION: esbuild clean on CourseDetail.tsx and CoachHub.tsx.
