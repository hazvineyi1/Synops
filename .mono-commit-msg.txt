Marketing: edtech-first sub-pages + kill stale service worker

Platforms and About pages reworded to lead with EdTech and AI (was ops/managed-care and 'Consulting Group' framing); healthcare kept as the supporting backbone. Neutralises a leftover PWA service worker from an old build that served a stale shell and 404'd deep links like /products: adds a self-destruct public/sw.js and an index.html killswitch that unregisters any old worker and clears caches. Server SPA fallback already serves index.html for client routes. Em-dash free. Files: paideia-ren (Platforms, About, index.html, public/sw.js).
