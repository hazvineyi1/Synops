Partner Accounts + Funders on live data (real members endpoint, real funding_agreements) (ASCII-only)

Accounts and Roles now lists the partner's REAL accounts from new GET /partners/:id/members (super admin any partner, partner_admin own); the manage drawer wires Reset password (POST /platform/users/:id/reset-link) and Suspend/Reactivate to the real endpoints for super admin. Funders Hub is now backed by a real funding_agreements table (new schema): list/add/edit/delete/status/conditions persist via GET/POST/PATCH/DELETE /partners/:id/funding (self-creates the table, additive; super admin any partner, partner_admin own). Seat allocation is derived read-only from funded seats pending learner-level assignment.

VALIDATION: esbuild clean on partners.ts, funder.ts, db index (funding_agreements exported), PartnerAccounts.tsx, PartnerFunders.tsx.
