Financial Hub on live data: real billing_subscriptions + billing_invoices (ASCII-only)

Partner Financial Hub is now backed by real tables. New billing router (mounted): GET /partners/:id/billing plus POST/PATCH/DELETE for /subscriptions and /invoices (self-create tables, additive; super admin any partner, partner_admin own). Subscriptions tab: real rows, add subscription (org + plan + price + seats), edit price/seats on blur, delete; monthly MRR derived. Invoicing tab: create invoice, edit net on blur, Mark paid (status), delete; VAT + totals derived. Funder Disbursement tab now derives from the REAL funding agreements. Tax/VAT derived from real invoices. New schema billing.ts (billing_subscriptions, billing_invoices).

VALIDATION: esbuild clean on billing.ts, routes/index.ts, db index (billing tables exported), PartnerFinance.tsx.
