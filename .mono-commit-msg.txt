Super admin console P4: platform Financial Hub + Filing cabinet (contracts/MOUs) (ASCII-only)

NO MIGRATION REQUIRED. Frontend only.

Adds /platform-finance under the Platform nav group: a Financials tab (platform-wide MRR / outstanding / funding / VAT rollup + per-partner table with platform totals) and a Contracts and MOUs filing cabinet (search + type/partner filters, upload/file a document, inline status, backed by the new platformFilingStore with seeded MSAs, MOUs, SLAs, DPA and funder agreements for both partners).

VALIDATION: local-resolution esbuild bundle clean on platformFilingStore, PlatformFinance, App, AppLayout.
