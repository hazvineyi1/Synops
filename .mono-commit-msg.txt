Learning Hub schema bootstrap endpoint (no psql needed) (ASCII-only)

Adds POST /api/learning/_migrate (super_admin only): runs the same additive/idempotent DDL + seed as praxis/migration-learning-hub.sql using the server's own DATABASE_URL connection, so the learning_content / course_templates / course_assignments tables can be created in environments without psql or DB-console access. Safe to call repeatedly (IF NOT EXISTS / ON CONFLICT DO NOTHING). Returns row counts.

VALIDATION: esbuild syntax-check clean on learningHub.ts.
