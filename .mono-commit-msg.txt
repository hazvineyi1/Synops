Production-readiness Section A: authz gates, credential/completion integrity, kill fake data (ASCII-only)

SECURITY (authz was per-route with holes): PATCH /coach/submissions/:id and PATCH /organisations/:orgId were requireAuth-only (a learner could grade any submission / rename any org) - now role+scope gated. billing/funder/documents/delegated-admin canManage checked tenant match but not role (learners carry partnerId) - now require a facilitator role. GET /organisations/:orgId/members, GET /coach/submissions (legacy branch) and GET /coach/learners/:userId/presession were unscoped learner-data reads - now scoped to the caller's learners via a shared viewableLearnerIds helper.

CREDENTIAL/COMPLETION INTEGRITY: the AI-grader outage fallback graded free text by word count high enough to mint a credential - capped below the mastery threshold so a real grader is always required. Module completion was client-asserted (POST /progress/beat marked any beat viewed) - quiz beats are now server-validated: they only count when the correct answer is submitted (ModuleViewer sends the answer; arrival no longer auto-marks quizzes). Funder report was platform-wide and counted invalid credentials + hardcoded competency data - now org-scoped, valid-only, no fabricated figures.

FAKE DATA / TRUST: getPartnerHub no longer falls back to the TalentForge demo tenant and HUBS is emptied, so MTN/Vodacom/TalentForge can never render for a real partner. CoachDashboard shows real learner/submission/readiness data (was hardcoded 24/7/82pct). Brand leaks removed from the exported report footer + WhatsApp invite. Seeded learner logins moved off the .test domain. Added a top-level React ErrorBoundary (no more white-screen on a render throw).

VALIDATION: esbuild clean across all changed backend + frontend files.
