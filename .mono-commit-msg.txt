Gamify + vary assignments; make the coach reflect off-track from low completion (ASCII-only)

ASSIGNMENTS: gamified the auto-graded challenge - the result now shows a Bronze/Silver/Gold tier with a medal, XP earned (+10 per correct), an animated reveal and a clearer pass message. Varied them by rotating a challenge THEME per module (Decision Challenge / Scenario Sprint / Best-Move Challenge / Reality Check) with themed titles, intros and rotated question order, so modules no longer look identical. Kept them auto-graded and in-module with optional upload.

OFF-TRACK / COACH: the coach said 'on track' even for learners far behind because off-track only looked at graded work. Added a low_completion signal (behind on module content -> off_track) to evaluateOffTrack; generateStudyPlan now falls back to a content plan built from the learner's incomplete modules when there are no graded gaps; and opening the Coach hub (/learn/coach/overview) now runs ensureLearnerCoachPlans - recomputing off-track per enrolled course and building the gap-targeted catch-up plan on the spot. So a behind learner immediately sees a real plan instead of a misleading 'on track'.

VALIDATION: esbuild clean on enzaEnrich.ts, gradebookEngine.ts, studyPlanEngine.ts, gradebookAlerts.ts, coachArea.ts, assignments.ts, AssignmentDetail.tsx.
