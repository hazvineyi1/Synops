Perf: parallelize /gradebook/mine + linear-time /platform/financials (ASCII-only)

/gradebook/mine computed each course's summary in a SERIAL loop (reconcile + columns + scores awaited per course), multiplying latency by the learner's course count - now runs the independent per-course work in Promise.all. /platform/financials filtered the whole subs/invoices/funding arrays once PER partner (O(partners x rows)) - now pre-indexes each dataset by partner in a single pass, so it's linear in rows+partners.

VALIDATION: esbuild clean on gradebook.ts and platform.ts.
