Partner audit trail on real data: new /partners/:id/audit endpoint + rewire PartnerAudit (ASCII-only)

New GET /partners/:partnerId/audit returns the real append-only audit trail (auditEventsTable) scoped to the partner, gated to super admin OR a facilitator of that partner, with actor names resolved and an action->category mapping (financial/funder/account/impersonation/branding). PartnerAudit's Activity Log now reads it (was the partnerHubData mock, which rendered blank), and the impersonation target picker lists the partner's REAL accounts from /partners/:id/members instead of the fake impersonatableUsers. Empty states added.

Only remaining mock partner surface: the client-side PartnerImpersonateView preview (redundant with the real /platform impersonation session).

VALIDATION: esbuild clean on partners.ts and PartnerAudit.tsx.
