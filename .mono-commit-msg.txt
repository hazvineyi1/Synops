Gradebook: render Pass/Fail and % in the coach matrix cells

The staff gradebook matrix showed every read-only cell as a numeric fraction. Now each cell formats per its grade type: Pass/Fail (>=0.5 = Pass), Completion as a %, Points as X/Y - matching the learner view. Auto-scored (from completion) cells carry a small * marker. Editable assignment/manual cells keep the numeric entry input. Files: pages/CourseGradebook.tsx.
