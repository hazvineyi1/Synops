Impersonation: View as opens the users own view - a learner shows their class (ASCII-only)

NO MIGRATION REQUIRED. Frontend only, seeded partner data.

Impersonation now actually shows what the user sees. New shared impersonationStore (active session + log) so a session persists across pages. Audit View as starts the session and navigates to /partner/impersonate/:orgId/:userId (PartnerImpersonateView) with a sticky Viewing-as banner and Exit. For a LEARNER it renders their learner view: their class(es) with coach and assigned courses, their courses with progress, grade/attendance/last-active, coach and on-track status. Coaches see the classes they coach; org/delegated admins get a link into the org hub. impersonatableUsers now carries orgId. Audit recent-sessions merges the live log; active banner offers Resume view / Stop.

VALIDATION: parse-check + local-resolution bundle clean; impersonate route registered.
