Enza learners: standardised test logins enza@student1..N.test with a shared password (ASCII-only)

The four seeded Enza cohort learners now get deterministic sign-in details so the founder can log in as each level. applyLearnerCredentials() sets their emails to enza@student1.test .. enza@studentN.test (ordered by creation, so student1 is the advanced learner, student4 the novice) and a shared password Enzatest123 (hashed server-side), status active. Runs on every Seed Enza Cohort click - the idempotent branch now refreshes the logins instead of skipping - and the button toast reports the exact emails + password.

VALIDATION: esbuild clean on enzaCohortSeed.ts, AdminPartners.tsx.
