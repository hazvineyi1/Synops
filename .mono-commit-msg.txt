Fix flow: finishing an activity returns to its section (with forward Next), not Overview (ASCII-only)

Finishing the Complete activities (or video/quiz player) dropped the learner back on the module Overview showing 'Start: Video', losing their place. The player now returns to the hub with ?tab=<section it was opened from> (video/readings/complete), and ModuleHubView reads an initialTab from the URL. So after finishing Complete you land on the Complete tab where the forward button reads 'Next: Case studies', keeping the linear order intact.

VALIDATION: esbuild clean on ModuleViewer.tsx.
