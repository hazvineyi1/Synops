Learning Hub persistence backend + create-course UI + ribbon/catalog polish (ASCII-only)

MIGRATION REQUIRED: run praxis/migration-learning-hub.sql against the Praxis Postgres (creates learning_content, course_templates, course_assignments + seeds). Additive/idempotent. Nothing calls those tables from the UI yet, so this deploy is safe even before the migration runs.

LEARNING HUB PERSISTENCE (backend, additive): Drizzle tables in lib/db schema/learning_hub.ts; new api-server route learningHub.ts (content GET/POST + upload to Supabase Storage, review, delete; templates GET/POST; assignments GET/PUT/toggle), writes gated to super_admin; src/lib/supabaseStorage.ts REST helper (SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY, storageEnabled()).

COURSE AUTHORING (real /courses): New course button on the catalog for author roles -> POST /courses -> opens the course. Tightened POST /courses to author roles only (was any authenticated user).

POLISH: super-admin ribbon lettering now solid white/bolder; org Courses tab shows an Assigned from the Learning Hub card.

ENV (for uploads later): SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY on the Praxis service + a Storage bucket named learning-hub.

VALIDATION: esbuild clean on Courses.tsx, courses.ts, learningHub route/schema/storage, AppLayout, PartnerOrgHub.
