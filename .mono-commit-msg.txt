Classes are now real + a much better assignment flow (org_classes backend) (ASCII-only)

The org Classes feature was 100 percent client-side seeded state (orgClassStore) that reset on reload and never reached the real course/roster system. Now real and persistent. New org_classes + org_class_learners/courses/staff tables and classes router: GET/POST /organisations/:id/classes, GET/PATCH/DELETE /classes/:id, PUT /classes/:id/learners|courses|staff (bulk replace), POST /classes/:id/enrol (materialises REAL enrolments for every class learner x class course). Org-scoped access (super admin, org's partner_admin, or the org's own admin); self-creates tables. UX overhaul in PartnerClassDetail: roster/courses/staff are searchable multi-selects with Select-all + one Save per set (was one-at-a-time toggles), an Enrol-into-courses action, delete-with-confirm, and links out to the real Coaching/Gradebook; dropped the fake gradebook/messages/activity tabs. PartnerOrgHub class list + create now hit the API.

VALIDATION: esbuild clean on classes.ts, routes/index.ts, db index (org_classes exported), PartnerOrgHub.tsx, PartnerClassDetail.tsx.
