Enza modules: fix assignment/workshop enum bug, readings count, forward nav, add Case Studies tab + slide images (ASCII-only)

Fixes the four issues on the built-out modules. (1) Assignments AND Workshop were greyed because submissionType 'project' is not a valid assignment_type enum value, so the insert threw and skipped everything after it - changed to 'file_upload' in enzaEnrich + enzaSeed. (2) Readings badge showed 10 (it counted the lesson slide-beats); it now counts actual reading DOCUMENTS (2), and the lesson-beat list is hidden in the Readings tab when documents exist. (3) Bottom-nav 'Next' now moves FORWARD through the flow (Readings -> Complete) instead of pointing back to an earlier unfinished section. (4) New Case Studies tab (its own tab in every module) backed by GET /modules/:id/cases, launching the Socratic case runner; and the narrated video slides now carry keyword-based freely-licensed images (LoremFlickr) rendered behind each slide. Enrich now re-upgrades modules built before slide images existed.

VALIDATION: esbuild clean on enzaEnrich.ts, enzaSeed.ts, cases.ts, platform.ts, ModuleViewer.tsx.
