Security: enforce the tuned Content-Security-Policy (was report-only)

Verified report-only produced zero violations across the app's core pages (dashboard, courses, grades, accounts), then broadened the policy to an LMS-realistic posture and flipped the header from Content-Security-Policy-Report-Only to Content-Security-Policy (enforcing). Route-aware frame-ancestors: app framed only by itself (anti-clickjacking) while /c and /a embeds stay externally embeddable. Pragmatic allowances so the learning flow can't break under enforcement: HTTPS media/uploads/embeds (Supabase storage audio+video, external images, framed content) + wss realtime; still blocks plugins (object-src none), base-uri and cross-origin form hijack. 'unsafe-inline' kept for the SPA's inline styles/bootstrap. One-line rollback: change the header name back to report-only. File: app.ts.
