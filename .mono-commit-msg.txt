Learner QA: grades reconcile, per-module completion, catalog links, video complete, dashboard, content rebuild (ASCII-only)

LEARNER GRADES (critical): the learner /grades endpoints (/courses/:id/gradebook/me and /gradebook/mine) never ran the reconcile the staff gradebook does, so graded assignments showed dashes and Overall said 'not enough data'. Both now reconcile submissions into gradebook_entries first.

PER-MODULE COMPLETION (critical): completion counted beat_progress by the denormalized module_id, so after a rebuild (new beat ids) every module collapsed to one shared percent. Progress + engine now JOIN through beats so only current beats count, attributed to their real module.

CATALOG LINKS (critical): GET /courses/:id gated on enrolment and 403'd every non-enrolled catalogue course (client showed 'Course not found'). Now allows catalogue-scope viewing (tenant-owned or partner-assigned); coursework stays enrolment-gated.

VIDEO COMPLETE: the slide-lesson never marked the video beat viewed; it now fires a completion when the learner reaches the final slide, so the Video tab earns its green check.

DASHBOARD: course badge/counter now driven by real percent, not a stale 'completed' status (no more Completed badge at 17% with 'in progress: 0').

CONTENT: bumped a CONTENT_VERSION so Build Full Courses rebuilds stale modules (regenerates fixed readings + distinct CFU q2 + case briefs) instead of skipping them.

UX: discussions scoped to the module (Participate tab); XP proportional to score (Jeopardy no longer pays 60 for a 50-pt game); hardened submission panel against raw JSON; module ?tab deep-link now persists on reload.

VALIDATION: esbuild clean across all changed files.
