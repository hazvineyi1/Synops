Generate course learning objectives on the course page (ASCII-only)

The Course learning objectives card (CourseDetail) gets a Generate button: pick Bloom's cognitive levels (Remember..Create), and one measurable objective is drafted per level from the course title/description using the existing courseDevEngine.generateObjectives, appended to the editor for review before Save. No new endpoint; reuses the client-side Bloom engine already used by the Course Development Suite.

VALIDATION: esbuild clean on CourseDetail.tsx.
