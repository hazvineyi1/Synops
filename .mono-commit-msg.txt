Seat-level funder assignment: assign learners to a grant's funded seats (ASCII-only)

New funded_seat_assignments table + endpoints in funder.ts: GET /partners/:id/funding-usage (used counts), GET/POST/DELETE /partners/:id/funding/:agreementId/seats (assign a learner to a funding agreement, capacity + duplicate guarded; self-create table). Funders Hub Seat Allocation tab now real: each agreement expands to its assigned learners with used/capacity progress, assign from the partner's real learners, and remove. Completion evidence can now attribute back to the right grant. New schema funded_seat_assignments.ts.

VALIDATION: esbuild clean on funder.ts, db index (funded_seat_assignments exported), PartnerFunders.tsx.
