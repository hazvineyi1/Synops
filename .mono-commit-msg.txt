Documents and Filing on live data: real partner_documents register (ASCII-only)

Partner Documents and Filing is now a real persistent register. New documents router (mounted): GET/POST/PATCH/DELETE /partners/:id/documents (self-create table, additive; super admin any partner, partner_admin own). Upload files -> each recorded as a filing entry (name, category, org, size, status); per-row status change (pending/filed/action-required) and delete persist; counts/filters from real data. New schema partner_documents.ts. File BYTES storage (virus scan + retention) noted as a further step needing storage creds.

VALIDATION: esbuild clean on documents.ts, routes/index.ts, db index (partner_documents exported), PartnerDocuments.tsx.
