Partner Hub Overview reads real partners; unbacked panels marked Sample (ASCII-only)

Platform Overview no longer shows the seeded TalentForge/SkillBridge demo. It now fetches real partners (GET /partners) and organisations (GET /organisations), rendering the partner list, per-partner org drill-in, and KPIs (partners, orgs, learners) from live data with an empty-state CTA to create the first partner. Financials/funders/coaching-health have no live backend, so those panels are clearly badged Sample data. partnerHubData: registerRealPartners + emptyHub fallback so opening a REAL partner's hub shows an empty, correctly-named hub instead of another partner's demo figures.

VALIDATION: esbuild clean on partnerHubData.ts, PlatformOverview.tsx.
