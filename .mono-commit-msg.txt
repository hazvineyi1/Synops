Audit: partner admin can impersonate anyone in their organisations except the super admin (ASCII-only)

NO MIGRATION REQUIRED. Frontend only, seeded partner data.

The Impersonation tab is now a working start/stop flow. New impersonatableUsers(hub) lists every tenant account across the partner orgs - org admins, coaches, delegated admins and learners - and the platform super admin is never a tenant account so it can never appear (shown explicitly with a locked note). Searchable list with View as; starting sets a time-boxed active session (amber banner + Stop), prepends it to the sessions log as active, and Stop records the duration. Policy cards (time-boxed / org-notified / auto-logged) retained.

VALIDATION: parse-check + local-resolution bundle clean; ImpersonationSession + impersonatableUsers exports and Eye/Search/StopCircle icons confirmed.
