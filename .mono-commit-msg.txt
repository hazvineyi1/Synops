Attached deliverables render ready-to-use inline: interactive activities + case launch cards (ASCII-only)

- Activities: each attached interactive now renders EMBEDDED and INTERACTIVE inline in the course Activities tab (no separate open step), with an Attached badge and a Completed badge once the learner submits (ActivityPlayer onSubmit). Full screen + Remove remain.
- Cases: each attached case now shows an inline launch card (header with title + Attached badge, a Start case button that opens the Socratic runner, plus Edit for instructors), instead of a bare link. Draft cases show Start disabled with a publish hint.

VALIDATION: esbuild bundle clean on CourseDetail.tsx.
