Prod-readiness: gate seeding tools, fix admin overflow, stop tab-title brand leak (ASCII-only)

SAFETY: the Provision/Build/Seed/Resync data-seeding buttons sat inline in the super-admin Partner Management header. Moved them behind a collapsed 'Internal tools' panel with a warning and a per-action confirm() prompt, so they can't be clicked by accident against a live tenant.

LAYOUT: that same header row overflowed the viewport and cut off 'Create partner'. The header now holds only 'Internal tools' + 'Create partner' (shrink-0), so nothing is clipped; the partner table already scrolls within its own container.

BRANDING: the browser tab title flipped to the base 'Synops Praxis' after navigation because it was only set when a brand displayName existed (stale value otherwise). ThemeProvider now always sets the title from the resolved brand, falling back to a neutral 'Praxis' rather than leaking the base platform name.

VALIDATION: esbuild clean on AdminPartners.tsx and ThemeProvider.tsx.
