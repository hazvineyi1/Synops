Financial Hub: editable figures (plan price, seats, invoice net, VAT rate) with live recompute (ASCII-only)

NO MIGRATION REQUIRED. Frontend only, seeded partner data.

The Financial Hub key figures are now editable inline via a NumInput helper: plan price per seat, per-org seat count, invoice net amount, and the VAT rate. Everything derived recomputes live - each org monthly total, the Monthly recurring KPI + a new Total row, Outstanding, invoice VAT/total columns (header shows the current rate), and VAT collected/pending. VAT rate has a reset-to-15% shortcut. Seeded/session only; persistence is a backend step.

VALIDATION: parse-check + local-resolution bundle clean; Pencil icon confirmed.
